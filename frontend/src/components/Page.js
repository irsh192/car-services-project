// src/components/Page.js
import React from 'react';

const Page = ({ title }) => {
  return (
    <div>
      <h2>{title}</h2>
      {/* Add content specific to each page */}
    </div>
  );
};

export default Page;
